#include "main.h"
/**
 * void print_array - this is what the main function does
 * @a: variable
 * @n: variable
 * Return: this is what's supposed to be returned to main
 */

void print_array(int *a, int n)


{

	while (*str != '\0')
	{
		_putchar (*str);
		str += 2;
	}
	printf ('n');

}
