#include "main.h"
/**
 * mul - this is what the main function does
 * @a: variable
 * @b: variable
 * Return: this is what's supposed to be returned to main
 */

int mul(int a, int b)


{
	c = a * b

	return (c);

}
