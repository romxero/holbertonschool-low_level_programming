#include "main.h"
/**
 * _islower - this is what the main function does
 *
 * Return: this is what's supposed to be returned to main
 */


int _islower(int c)
{
	if (c == 'c')
	return (1);
	else

	return (0);

}
