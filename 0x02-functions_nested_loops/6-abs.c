#include "main.h"
/**
 * _abs - this is what the main function does
 * @int: variable
 * Return: this is what's supposed to be returned to main
 */

int _abs(int)

int x;

{
	if (x < 0)
	{
	x * -1
	_putchar(x);
	return (x);
	}
	else
	{
	_putchar('x');
	return (x);
	}

}
