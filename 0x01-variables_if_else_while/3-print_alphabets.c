#include <stdio.h>
/**
 * main - this is what the main function does
 *
 * Return: this is what's supposed to be returned to main
 */

int _islower(int c);


{
	if (c >= 'a' && c <= 'z')
	return (1);

	else
	return (0);


}
